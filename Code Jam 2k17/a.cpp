#include <bits/stdc+.h>

using namespace std;

inline int print(int a, int c){
	cout << "Case #" << c << ": " << a << endl;
}

int main(){
	int t;
	for(int i = 0; i < tt; i++){
		string inp;
		cin >> inp;
		int s = inp.size();
		std::vector<int> vi(s, 0);
		int cnt = 0;
		for(int i = 0; i < inp.size(); i++){
			int cnt = vi[i];
			if((inp[i] == '+' && cnt % 2 == 0) || (inp[i] == '-' && cnt % 2 == 1)) continue;
			else{
				
				int to = min()
			}
		}
	}
}